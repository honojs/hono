locale: "en"
repository: ${GITHUB_REPOSITORY}/perf-measures
coverage:
  if: false
codeToTestRatio:
  if: false
testExecutionTime:
  if: false
report:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
summary:
  if: true
customMetrics:
  bundle-size-check:
    key: bundle-size-check
  diagnostics-tsc:
    key: diagnostics-tsc
  diagnostics-tsgo:
    key: diagnostics-tsgo